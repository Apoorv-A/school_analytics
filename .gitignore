# Environment / secrets
.env
*.env
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local database
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite3

# Tooling installed locally by setup scripts
tools/

# The Java platform and its tenant configuration live in their own repositories:
#   Apoorv-A/school-analytics-platform   (platform/, including the oracle harness)
#   Apoorv-A/school-analytics-config     (config/)
# They are checked out here side by side because the oracle harness reads this application to
# capture the golden chart output the Java port is verified against.
platform/
config/

# OS / editor
.DS_Store
.idea/
.vscode/
